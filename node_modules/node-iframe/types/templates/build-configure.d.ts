import { TemplateType } from "./config";
declare type ConfigureTemplates = (template: string, type: TemplateType) => void;
declare const configureTemplates: ConfigureTemplates;
export { configureTemplates };
